<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="io.playseek.plugin.pedometer">
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.BIND_JOB_SERVICE" />

    <application>
        <receiver android:name="io.playseek.plugin.pedometer.PedometerBroadcastReceiver"
                  android:enabled="true"
                  android:exported="true"/>
        <receiver android:name="io.playseek.plugin.pedometer.PedometerRebootBroadcastReceiver"
                  android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
            </intent-filter>
        </receiver>

        <service android:name="io.playseek.plugin.pedometer.PedometerService"
                 android:permission="android.permission.BIND_JOB_SERVICE"
                 android:exported="true"/>
    </application>
</manifest>
